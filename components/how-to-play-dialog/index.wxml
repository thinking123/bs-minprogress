<view class="wrapper" wx:if="{{visible}}" bindtap="handleOutHide">
    <view class="dialog-mask" id="mask"></view>
    <image src="{{url}}bg.png" class="dialog-bg" wx:if="!!src"/>
    <image src="{{url}}return-btn.png" class="return-btn" bindtap="handleHide"/>
</view>
